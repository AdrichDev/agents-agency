name: CI

on:
  push:
    branches: [master]
  pull_request:

# Cancela ejecuciones previas del mismo ref si llega un push nuevo.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  back:
    name: Backend (typecheck + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: back
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: back/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      # Supply-chain (pilar 6): visibilidad de CVEs, no bloqueante.
      - run: npm audit --audit-level=high
        continue-on-error: true
      # Código muerto: visibilidad continua, no bloqueante.
      - run: npx --yes knip@5 --no-progress --include files,dependencies
        continue-on-error: true

  front:
    name: Frontend (typecheck + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: front
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: front/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      # Supply-chain (pilar 6): visibilidad de CVEs, no bloqueante.
      - run: npm audit --audit-level=high
        continue-on-error: true
