# Tasks — WhatsApp Webhook Fail-Closed

## Fase 1
- [x] 1.1 `channels.ts`: webhook WhatsApp fail-closed (403 si no se puede verificar firma)

## Fase 2
- [x] 2.1 `back`: `tsc --noEmit` + `vitest` (352) verdes
- [x] 2.2 Arranque real: server boota limpio
